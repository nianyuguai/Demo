#include <stdio.h>
#include <stdlib.h>

typedef struct Node
{
    int data;
    struct Node* left;
    struct Node* right;
}NodeTree;

NodeTree* makeEmpty(NodeTree* Tree)
{
    if(Tree != NULL)
    {

    }
}

int main()
{

    printf("Hello world!\n");
    return 0;
}
